{
  "name": "mmmbc",
  "private": true,
  "type": "module",
  "scripts": {
    "build:cf": "powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/build_cf_site.ps1",
    "test": "node --test src/site-editor.test.mjs",
    "verify:integrations": "node ./scripts/verify_integrations.mjs",
    "deploy": "npm run build:cf && npx wrangler deploy --env production"
  }
}
